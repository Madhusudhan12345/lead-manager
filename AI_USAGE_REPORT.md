# Mandatory AI Usage Report

**AI TOOL USED:** Claude

**WHAT I ASKED AI TO DO:**
1. Compare the five assignments and recommend which one I could finish well before the deadline.
2. Generate the backend (FastAPI + SQLAlchemy + SQLite), the single-page frontend, automated tests and the README.
3. Help me install, run, push to GitHub and deploy the project on Render, step by step.

**PROMPT THAT WAS MOST USEFUL:**
I uploaded the assignment PDF and asked which of the five problems I could build properly, test and deploy before the 9 AM deadline. Then I asked for the complete project files and step-by-step CMD commands to run it.

**CODE GENERATED BY AI:** Backend API, status transition rules, duplicate detection, dashboard aggregation, frontend page, tests, README.

**CODE I MODIFIED:** requirements.txt (relaxed the package versions, see below). I also added my live Render link to the README and re-created static/index.html on GitHub to fix its folder path.

**AI OUTPUT THAT WAS WRONG:**
The AI pinned old exact versions in requirements.txt (for example pydantic==2.9.2). They did not work on my computer.

**HOW I IDENTIFIED THE PROBLEM:**
pip install failed with "Failed building wheel for pydantic-core". The error showed my Python was 3.14 (cp314), and the old pydantic-core had no pre-built package for it, so pip tried to compile it with Rust and failed.

**HOW I FIXED IT:**
I changed requirements.txt to newer minimum versions (pydantic>=2.12, sqlalchemy>=2.0.44), installed with pre-built packages only, then confirmed that all 9 tests pass and the app runs locally and on Render.
